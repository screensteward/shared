name: screen_steward_shared
description: Protocol Buffers schemas, i18n bundles, and shared types for ScreenSteward.
version: 0.0.1-dev
repository: https://github.com/screensteward/shared
homepage: https://screensteward.app
issue_tracker: https://github.com/screensteward/shared/issues

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  protobuf: ^6.0.0
  fixnum: ^1.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
